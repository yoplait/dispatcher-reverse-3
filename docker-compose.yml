version: "3"

services:

    dispatcher:
        build: .
        ports:
            - 80:80
        networks:
            - web
            
    site1:
        build: ./site1/
        tty: true
        entrypoint:
            - /run.sh
            - /bin/bash
        ports:
          - "81:81"
        networks:
            - web

networks:
    web: